<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
  <head>
    <title>Cheat News</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <center>
      <table width="85%">
        <tbody>
          <tr>
            <th>
              <%= nav_bar static: :top, brand: "Cheat News", responsive: true do %>
                  <%= menu_group do %>
                      <%= menu_item "New", newest_path %>
                      <%= menu_item "Ask", asks_path %>
                      <% if current_user %>
                        <%= menu_item "Threads", threads_path %>
                      <% end %>
                      <%= menu_item "Submit", home_newsubmission_path %>
                  <% end %>
                  <%= menu_group pull: :right do %>
                      <% if current_user %>
                          <%= menu_text "Signed in as: " %>
                          <%= menu_item current_user.name, root_path %>
                          <%= menu_item "Sign Out", signout_path %>
                      <% else %>
                          <%= menu_item "Sign in with Facebook", "/auth/facebook" %>
                      <% end %>
                  <% end %>
              <% end %>
            </th>
          </tr>
          <tr>
            <td>
              <%= yield %>
            </td>
          </tr>
        </tbody>
      </table>
    </center>
  </body>
</html>